<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>

    {{#each styles}}
    <link rel="stylesheet" href="cs/{{this}}">
    {{/each}}
</head>

<body>
    {{{body}}}

    {{#each scripts}}
    <script src="js/{{this}}"></script>
    {{/each }}
</body>

</html>